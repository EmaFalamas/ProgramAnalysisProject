/* This very simple program will read a number from the standard input and use some (contrived)
 way to make sure the variable is not set to zero when the execution reaches the write
 statement. Will the analysis reflect this?*/

{
    int x;
    read x;

    if (x <= 0){
       x = x - 1;
    } else {
       x = 1;
    }

    write x;
}
